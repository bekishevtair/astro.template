---
import { localizePath, localizeUrl } from "astro-i18next"
const { items } = Astro.props
---

{
  items.map((item) => (
    <div class="nav__exp--item">
      <a
        class="nav__exp--link"
        href={localizePath(item.link)}>
        <span>{item.title}</span>
      </a>
    </div>
  ))
}
